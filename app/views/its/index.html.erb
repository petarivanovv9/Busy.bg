<a href = "/ads/new">  Направи обява </a>

<%= form_tag do %>
<div class="field">
    <%= label_tag :city_id %><br />
    <%=select_tag "city", options_from_collection_for_select( @cities, "id", "name")%>
  </div>
   <div class="field">
    <%= label_tag :ad_activities %><br />
    <%=select_tag "activity", options_from_collection_for_select( @activities, "id", "name"),  {:multiple => true} %>
  </div>
  </div>

  <div class="actions">
    <%= submit_tag("Search") %>
  </div>
<% end %>

<br />

<% @its.each do |it| %>
	<%= it.name %>
	<br />
<% end %>

