<%= form_for(@ad) do |f| %>
  <% if @ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad.errors.count, "error") %> prohibited this ad from being saved:</h2>

      <ul>
      <% @ad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <center>
  </br>
   <section class="main">
  
  <div id = "dd" class="field">
    Град:<br />
    <%= f.select :city_id, City.all.collect {|x| [x.name, x.id]}, {}, { :class => 'wrapper-dropdown-5' } %>
  </div>
  </br>
  <div id = "dd" class="field">
     Дейности:<br />
    <%= f.select :ad_activities, Activity.all.collect {|x| [x.name, x.id]}, {}, { :class => 'c'wrapper-dropdown-5''}%>
  </div>
  </br>
   <div class="field">
    Цена за час:<br />
    <%= f.text_field :max_price, :class => 'design11' %>
  </div>
  </br>
 
  <div class="field">
    Период:<br />
    <%= f.text_field :period, :class => 'design11' %>
  </div>
  </br>
  <div class="field">
    Описание:<br />
    <%= f.text_area(:description, :size => '30x2', :maxlength => "150", :class => 'design11')%>
  </div>
  </section>
  <div class="actions">
    <input type="submit" value="Създай", class="exit">
    
  </div>
</center>
<% end %>

